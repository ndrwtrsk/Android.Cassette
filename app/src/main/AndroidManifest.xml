<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="andrewtorski.casette">

    <application
        android:name=".app.AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".app.view.activity.ListCassettesActivity"
            android:label="@string/title_activity_list_cassettes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.view.activity.CassetteDetailActivity"
            android:label="@string/title_activity_cassette_detail"></activity>
    </application>

</manifest>
